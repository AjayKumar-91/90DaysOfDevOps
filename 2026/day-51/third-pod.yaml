apiVersion: v1
kind: Pod
metadata:
    name: custom-pod
    labels:
        app: custom-app
        environment: staging
        team: devops
spec:
    containers:
    - name: busybox
        image: busybox:latest
        command: ["sh", "-c", "echo Hello from custom pod && sleep 3600"]
