apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  clusterIP: None 
  selector:
    app: nginx-label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
    