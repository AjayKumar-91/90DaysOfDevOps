apiVersion: v1
kind: Pod
metadata:
  name: pending-demo
spec:
  containers:
  - name: busybox
    image: busybox
    command: ["sh", "-c", "sleep 3600"]
    resources:
      requests:
        cpu: "100"
        memory: "128Gi"